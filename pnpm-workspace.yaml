packages:
  # clients apps
  - "apps/clients/cribstop/next"
  # backend services that declare their own runtime dependencies, so that
  # `nx run <svc>:prune` can emit a pruned package.json + lockfile for a slim
  # production image (those targets require a project-level package.json)
  - "apps/services/property-service"
  # shared libs for web and mobile apps
  - "libs/listing-contracts"
  # - "libs/expo-next"
  # - "libs/next"
  # - "libs/expo"

catalog:
  # Web CSS pipeline — shared across all Next.js/web apps
  tailwindcss: "^3.4.17"
  autoprefixer: "^10.4.20"
  postcss: "^8.4.49"
