# Fixture: a multi-document manifest. The second document carries a lowercase key, which the
# derivation must qualify with the secret name rather than expose as a bare `auth` variable.
apiVersion: v1
kind: Secret
metadata:
  name: beta-secret
type: Opaque
stringData:
  BETA_PASSWORD: StrongBase64Password
---
apiVersion: v1
kind: Secret
metadata:
  name: beta-basic-auth-secret
type: Opaque
stringData:
  auth: admin:$2y$10$notarealhash
