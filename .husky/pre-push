# Run full checks before push (format + lint + type + test + build)
# Enforced automatically on protected branches (dev/test/main) only.
# Feature branches skip intentionally (fast iteration) — run `pnpm run pre-push`
# yourself before pushing.
# Skip nx:reset to avoid modifying files during push
node scripts/pre-push.js --hook --skip-reset
