# listing-contracts

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build listing-contracts` to build the library.

## Running unit tests

Run `nx test listing-contracts` to execute the unit tests via [Jest](https://jestjs.io).
