apiVersion: v1
kind: Service
metadata:
  name: inference-svc
  labels:
    app: inference
spec:
  type: ClusterIP
  selector:
    app: inference
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  sessionAffinity: None
