apiVersion: v1
kind: Service
metadata:
  name: property-service-svc
  labels:
    app: property-service
spec:
  type: ClusterIP
  selector:
    app: property-service
  ports:
    - name: http
      protocol: TCP
      port: 3002
      targetPort: 3002
  sessionAffinity: None
